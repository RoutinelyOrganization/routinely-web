import { styled } from "styled-components";

export const ContainerIconsStyle = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 24px;

  & > svg {
    cursor: pointer;
  }
`;
